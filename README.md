# iframes-with-angular
This repository demonstrates the use of iframes within angular and uses cases  like communication between the iframe and the parent window , and putting components within iframes
